/* __LABEL_EN__ feature (public/partials/__ID__.html). Declares ONLY the
 * deviations from the shared card vocabulary (DESIGN.md → Card interior,
 * Besitzer-Regel in public/css/CLAUDE.md). */

@layer components {
.__ID__-list {
  margin: 0;
  padding-left: var(--space-lg);
}
}
