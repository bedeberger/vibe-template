{
  "name": "vibe-template",
  "version": "0.1.0",
  "description": "Self-hosted SPA template — Node/Express + better-sqlite3 + Alpine.js (no build step). Facades, job queue, migrations, i18n, design tokens.",
  "license": "MIT",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20 <26"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node scripts/with-env.js LOCAL_DEV_MODE=1 -- node --watch server.js",
    "db:migrate": "node scripts/migrate.js",
    "test": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:e2e-app",
    "test:unit": "node scripts/with-env.js NODE_ENV=test -- node --test --test-concurrency=4 \"tests/unit/**/*.test.js\" \"tests/unit/**/*.test.mjs\"",
    "test:integration": "node scripts/with-env.js NODE_ENV=test -- node --test --test-concurrency=4 --test-force-exit \"tests/integration/*.test.js\"",
    "test:e2e": "playwright test",
    "test:e2e-app": "playwright test --config=playwright.app.config.js",
    "test:smoke": "playwright test --config=playwright.app.config.js tests/e2e-app/smoke.spec.js",
    "squash:check": "node --test \"tests/unit/squash-drift.test.mjs\"",
    "migrations:lock": "node scripts/migrations-lock.js --write",
    "migration:renumber": "node scripts/migration-renumber.js",
    "vendor:sync": "node scripts/vendor-sync.js",
    "feature:new": "node scripts/feature-new.js",
    "init": "node scripts/project-init.js"
  },
  "devDependencies": {
    "@alpinejs/anchor": "^3.15.12",
    "@alpinejs/collapse": "^3.15.12",
    "@alpinejs/focus": "^3.15.12",
    "@alpinejs/resize": "^3.15.12",
    "@playwright/test": "^1.49.0",
    "alpinejs": "^3.14.0",
    "chart.js": "^4.5.1",
    "sortablejs": "^1.15.6"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "better-sqlite3-session-store": "^0.1.0",
    "compression": "^1.7.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "helmet": "^8.0.0",
    "openid-client": "^5.7.0",
    "winston": "^3.17.0"
  }
}
